# Runtime data — real house data lives here. Never committed. (CLAUDE.md §12)
/data/

node_modules/
dist/
*.log
.DS_Store
.env
.env.*

# Generated by `npx tsx server/scripts/make-fixture.ts`. Reproducible on demand,
# and the tests build their own copy — committing it would let it drift.
/fixtures/synthetic/
